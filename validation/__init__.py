"""Reviewer-response validation package for EVAonline (EMS submission).

Reproducible scripts that generate the tables and figures requested by
Reviewer 1. Each ``r1cN_*.py`` script maps to one reviewer comment and
writes its artefacts (CSV + LaTeX snippet) to ``validation/outputs/``.

All scientific metrics live in :mod:`validation.metrics`.
"""
